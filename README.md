# VERY simple Flask todo app 


windows:
```bash
python -m venv .env

.\.env\scripts\activate

pip install -r requirements.txt

python .\app.py
```

linux:
```bash
python -m venv .env

source .env/bin/activate

pip install -r requirements.txt

python app.py
```

